image: node:9.11.1

pages:
  cache:
    paths:
    - node_modules/

  script:
  - cd website
  - yarn install
  - yarn build
  - mv build public

  artifacts:
    paths:
    - public
  
  only:
  - master
